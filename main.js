document.addEventListener('DOMContentLoaded',()=>{const t=document.getElementById('themeToggle'),b=document.body;if(localStorage.getItem('theme')==='light')b.classList.add('light-mode');t.addEventListener('click',()=>{b.classList.toggle('light-mode');localStorage.setItem('theme',b.classList.contains('light-mode')?'light':'dark')});const items=document.querySelectorAll('.bento-item');items.forEach(item=>{item.addEventListener('mousemove',e=>{const r=item.getBoundingClientRect();item.style.setProperty('--mouse-x',`${e.clientX-r.left}px`);item.style.setProperty('--mouse-y',`${e.clientX-r.top}px`)})});setTimeout(()=>b.classList.remove('no-transition'),100)});
